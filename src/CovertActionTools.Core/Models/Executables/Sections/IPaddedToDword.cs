namespace CovertActionTools.Core.Models.Executables.Sections
{
    /// <summary>
    /// When used in a section, indicates that the section's data should be padded to a full
    /// dword (4 bytes) when serialized, and that the padding after it is part of the
    /// section when deserialized.
    /// </summary>
    public interface IPaddedToDword
    {

    }
}
